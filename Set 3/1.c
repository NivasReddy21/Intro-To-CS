#include<stdio.h>
#include<math.h>


int main()

{

    int a;
    float squart;

    printf("Enter a no : ");
    scanf("%d", &a);
    squart = sqrt(a);
    printf("Square root = %f ", squart);  
    return(0);
}